import React from 'react';
import ExampleDescription from 'components/ExampleDescription';

const renderComp = renderFactory(ExampleDescription, {
});

describe('ExampleDescription', function() {
  it('renders as a <replace>', function() {
    expect(true).to.be.true;
  });
});
