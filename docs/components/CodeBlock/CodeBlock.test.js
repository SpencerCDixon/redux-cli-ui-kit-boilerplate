import React from 'react';
import CodeBlock from 'components/CodeBlock';

const renderComp = renderFactory(CodeBlock, {
});

describe('CodeBlock', function() {
  it('renders as a <replace>', function() {
    expect(true).to.be.true;
  });
});
