import React from 'react';
import CompLayout from 'components/CompLayout';

const renderComp = renderFactory(CompLayout, {
});

describe('CompLayout', function() {
  it('renders as a <replace>', function() {
    expect(true).to.be.true;
  });
});
