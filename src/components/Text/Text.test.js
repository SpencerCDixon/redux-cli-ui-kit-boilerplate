import React from 'react';
import Text from 'components/Text';

const renderComp = renderFactory(Text, {
});

describe('Text', function() {
  it('renders as a <replace>', function() {
    expect(true).to.be.true;
  });
});
